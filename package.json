{
  "name": "robot-face-mapper",
  "version": "1.0.0",
  "private": true,
  "description": "Robot Face Mapper - Vite static site for GitHub Pages",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.0"
  }
}